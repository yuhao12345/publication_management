article(ID int pkey, title string not null, subject string, length int, photo_num int, status boolean, notes string)
journal(name String pkey,abbr String,impact_factor double,if_open_source boolean,acception_rate double)
Editor(ID int pkey,name String, working_year int)
author(ID int pkey,first_name string not null, last_name string not null, email string)
affliation(ID int pkey,name string not null,country string,established_year int,description string)
company(name string pkey,established_year int,country_of_origin string)
keyword(name string pkey,popularity string)
publishing(ID int pkey, joural_name string fkey, article_name string fkey, article_ID int fkey)
journal_editor(journal_name string fkey, Editor_name string fkey,primary key(journal_name,Editor_name))
owned(company_name string fkey, journal_name string fkey, primary key(company_name,journal_name))
writing(ID int pkey,article_ID int fkey, author_ID int fkey)
employer(ID int pkey, author_ID int fkey, affliation_ID int fkey)
article_key(article_ID int fkey, keyword_name string fkey, primary key(article_ID,keyword_name))
evaluate(ID int pkey, Editor_ID int fkey, article_ID int fkey, notes String)